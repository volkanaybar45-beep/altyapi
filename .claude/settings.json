{
  "model": "opus",
  "permissions": {
    "allow": [
      "Bash(git add:*)",
      "Bash(git commit:*)",
      "Bash(git status)",
      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Bash(python3:*)",
      "Bash(ls:*)",
      "Bash(cat:*)",
      "Bash(grep:*)",
      "Bash(find:*)",
      "WebSearch"
    ],
    "deny": [
      "Bash(rm -rf:*)",
      "Bash(git push:*)",
      "Bash(git reset --hard:*)",
      "Bash(git clean:*)",
      "Read(./export_presets.cfg)",
      "Read(./*.keystore)",
      "Read(./*.jks)",
      "Read(./.env)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import json,sys; d=json.load(sys.stdin); print(d.get('tool_input',{}).get('file_path','') or d.get('tool_response',{}).get('filePath',''))\" | { read -r f; f=${f//\\\\//}; r=$(git -C \"$(dirname -- \"$f\")\" rev-parse --show-toplevel 2>/dev/null); [ -n \"$r\" ] && cd \"$r\" || exit 0; git add -A; git diff --cached --quiet && exit 0; b=$(basename -- \"$f\"); git commit -m \"Otomatik commit: $b guncellendi - $(date '+%Y-%m-%d %H:%M')\" && (GIT_TERMINAL_PROMPT=0 timeout 15 git push origin HEAD >/dev/null 2>&1 || true); } 2>/dev/null || true",
            "timeout": 30
          },
          {
            "type": "command",
            "command": "python -c \"import json,sys; d=json.load(sys.stdin); print(d.get('tool_input',{}).get('file_path','') or d.get('tool_response',{}).get('filePath',''))\" | { read -r f; case \"$f\" in *.gd|*.tscn|*.tres|*.godot) ;; *) exit 0 ;; esac; f=${f//\\\\//}; d=$(dirname -- \"$f\"); p=\"\"; for i in 1 2 3 4 5 6 7 8; do [ -f \"$d/project.godot\" ] && { p=\"$d\"; break; }; d=$(dirname -- \"$d\"); done; [ -n \"$p\" ] || exit 0; export APPDATA=\"C:/Users/VOLKAG~1/AppData/Local/Temp/claude/godot_hook_appdata\"; mkdir -p \"$APPDATA\"; G=\"C:/DevTools/Godot/4.7.1/Godot_v4.7.1-stable_win64_console.exe\"; case \"$f\" in *.gd) rel=${f#$p/}; out=$(timeout 120 \"$G\" --headless --path \"$p\" --check-only --script \"$rel\" 2>&1) ;; *) out=$(timeout 120 \"$G\" --headless --path \"$p\" --import 2>&1) ;; esac; out=$(printf '%s' \"$out\" | grep -iE \"SCRIPT ERROR|Parse Error|ERROR: |Failed to load script\" | head -15); if [ -n \"$out\" ]; then echo \"GODOT IMPORT KAPISI - hata var, duzelt:\"; echo \"$out\"; exit 2; fi; exit 0; }",
            "timeout": 150
          }
        ]
      }
    ]
  }
}